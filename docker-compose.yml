services:
  copilot-cli:
    build: .
    container_name: copilot-cli
    init: true
    ports:
      - "127.0.0.1:2222:22"
    volumes:
      # Mount your SSH public key for auth
      - ~/.ssh/id_ed25519.pub:/tmp/authorized_keys:ro
